<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ysu.repository.UserRepository">

    <select id="queryAllUser" resultType="User">
        select * from tb_user
    </select>

    <select id="login" parameterType="User" resultType="User">
        select * from tb_user where userName = #{userName} and password = #{password}
    </select>

    <select id="queryUserById" resultType="User">
		select * from tb_user where userId = #{userId}
	</select>

    <select id="queryRoleIdsByUserId" resultType="java.lang.Integer">
		select roleId from tb_user_role where userId = #{userId}
	</select>

    <insert id="insertUserRoles">
        insert into tb_user_role (userId, roleId) values
        <foreach collection="roleIds" item="roleId" separator=",">
            ( #{userId}, #{roleId} )
        </foreach>
    </insert>

    <delete id="deleteUserRoles">
        delete from tb_user_role where userId = #{userId} and roleId in
        <foreach collection="roleIds" item="roleId" separator="," open="(" close=")">
            #{roleId}
        </foreach>
    </delete>

    <delete id="deleteUser" parameterType="integer">
        delete from tb_user where UserId = #{userId}
    </delete>

    <select id="validateLoginName" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT  userName FROM tb_user WHERE userName=#{userName}
    </select>
    <select id="getUserByActive" resultType="com.ysu.entity.User">
        SELECT * FROM tb_user WHERE active = #{activeCode}
    </select>

    <insert id="saveUser" parameterType="com.ysu.entity.User">
      <selectKey  keyProperty="userId" order="AFTER" resultType="java.lang.Integer">
          SELECT LAST_INSERT_ID()
      </selectKey>
        INSERT INTO tb_user (userName, password, userTrueName, createDate, email, active) VALUES (#{userName}, #{password}, #{userTrueName}, #{createDate}, #{email}, #{active})
    </insert>

    <insert id="insertUser" parameterType="User">
        <selectKey  keyProperty="userId" order="AFTER" resultType="java.lang.Integer">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert tb_user(userName,password,userTrueName,active) values(#{userName},#{password},#{userTrueName},'0')
    </insert>

    <update id="active">
        UPDATE tb_user set userState=0,active='' WHERE active=#{activeCode}
    </update>

    <update id="updateUser" parameterType="User">
        update tb_user set userName = #{userName}, userTrueName = #{userTrueName} where UserId = #{userId}
    </update>
</mapper>