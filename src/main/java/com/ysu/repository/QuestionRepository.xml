<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ysu.repository.QuestionRepository">


	<select id="queryAllQuestions" resultType="Question">
		select * from tb_question
		<where>
			<if test="examCode != null">
				examCode = #{examCode}
			</if>
			<if test="keyword != null">
				and queInfo like '%' #{keyword} '%'
			</if>
		</where>
	</select>
	<select id="queryQuestionsByIds" resultType="Question">
		select * from tb_question where id in
		<foreach collection="array" item="id" open="(" separator="," close=")">
			#{id}
		</foreach>
	</select>

	<select id="queryQuestionsByPaperId" resultType="Question">
		select * from tb_question where id in (select questionId from tb_paper_question where paperId = #{paperId})
	</select>

	<insert id="insertQuestion" parameterType="Question">
		insert into tb_question(queInfo,queType,queScore,optA,optB,optC,optD,answer,examCode,createDate) values
		(#{queInfo},#{queType}, #{queScore},#{optA},#{optB},#{optC},#{optD},#{answer},#{examCode},#{createDate})
	</insert>

	<update id="updateQuestion" parameterType="Question">
		update tb_question set queInfo = #{queInfo},queType=#{queType},queScore=#{queScore},optA=#{optA},optB=#{optB},optC=#{optC},optD=#{optD},answer=#{answer},examCode=#{examCode} where id = #{id}
	</update>

	<delete id="deleteQuestion" parameterType="integer">
		delete from tb_question where id = #{id}
	</delete>

</mapper>